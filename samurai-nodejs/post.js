const { request, response } = require("express");
const express = require("express");
const server = express();

server.use(express.type());

let customers = [
    { id: 1, name: "" },
]


app.post("/"), (request, response) => {
    const { name } = request.body;
}


app.listen("3000")
